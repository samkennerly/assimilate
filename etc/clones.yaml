# GitHub repositories

folder:
  # Clone to this folder
  "~/code"

giturl:
  # Base URL for GitHub
  "https://github.com"

remote:
  # {local:remote} for each repo
  python/blackbox:
    samkennerly/blackbox
  bash/dockerbash:
    samkennerly/dockerbash
